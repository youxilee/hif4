# 版本记录

## v1.7 — 三个负结果记录（Attention 结构空间已基本挖完）

### 尝试但放弃

1. **Q/K 居中×平滑×置换 联合网格搜索**：在真实 GPT-2 前 3 层上枚举
   2 种居中 × 5 个平滑 d × 4 种置换的完整网格（40 组合/层，仅
   87ms/层），发现最优 d 对所有置换完全不变（每层各置换最优 d 的
   均值一致），两阶段贪心 + v1.4 置换基已覆盖网格最优；center=2 在
   全部 3 层未赢过 identity。联合搜索无空间。
2. **校准样本数 1→6（固定同一测试集）**：q/k/v/o/fc 全部噪声级波动，
   attn 仅 +0.010（1→4 样本）；校准数据量不是主要瓶颈。唯一异常是
   proj 分数在 0.27~0.41 间大幅波动（GELU 输出统计不稳定），但
   不是样本数问题。
3. **proj 激活满 Gram（`_ACTIVATION_QUADRATIC_MAX_FEATURES` 提到
   3072）**：proj 0.4009→0.4005（-0.0004），其余全部不变。3072 宽层
   的激活 Gram 不优于对角重要性，保持 1024 保守上限（状态 4096 元素）。

结论：Attention 的结构空间（居中/平滑/置换/重要性）已基本挖完，
当前方案在 GPT-2 12 层口径下接近饱和。

## v1.6 — refine offset 循环批量化（零行为变化）+ 预算权衡实测

### 改动

`_dense_to_hif4` 的 offset 精确求解从逐 offset 循环改为一次性批量：
把 hard 块沿 offset 维展开成 `[K, N]` 一次求解后按块 argmin；配套把
`_solve_exact_hierarchy` 的 einsum 与 mantissa gather 改为支持批量维。
行为与 v1.5 完全一致（12 层得分逐项相同，`test_solution.py` 通过），
精确求解调用次数减半（动态激活 8→4 次/样本）。实测对 13ms 的动态
耗时收益不大（Gram einsum 仍是主开销），但代码路径更简洁且后续可扩展。

### 预算权衡实测（重要）

数据驱动 refine 比例在真实 GPT-2 上高达 0.92~0.97——99% 损失覆盖目标
下几乎全量 refine。`_RATIO_CAPTURE_TARGET` 的分数代价（相对 0.99）：

| target | q | o | proj | attn |
| --- | --- | --- | --- | --- |
| 0.95 | -0.0008 | -0.0019 | -0.0019 | -0.0095 |
| 0.90 | -0.0019 | -0.0045 | -0.0037 | -0.0162 |
| 0.80 | -0.0044 | -0.0111 | -0.0074 | -0.0315 |

结论：0.99 是分数最优；若测试集极大（N=10×64 时动态外推约 15 分钟，
超 5 分钟预算），需接受上述分数损失换时间，建议优先降 attn 相关
比例（代价最重）之前先降 Linear 的比例。

## v1.5 — V 的 head 级注意力概率加权重要性（attn +0.0010）

### 动机

V 量化此前没有任何重要性：`v_state` 只有 offsets 和 refine 比例。
但 V 的误差进入 softmax 输出时被注意力权重 A 放大，输出 MSE 的静态
权重近似为 `E[A^2]`。head_dim=64 时一个 HiF4 64 块恰好是一个 KV head
的一条位置切片，按 head 加权可直接作用到块上。与 Q/K 协方差不同，
这是静态概率统计（因果 softmax 的平方平均），不存在少量 token 过拟合。

### 改动

`_V_ATTENTION_IMPORTANCE`：校准期统计每个 KV head 的 `E[A^2]`
（GQA 取组内 Q head 平均），均值归一化后作为 V 的通道重要性，
同时用于 refine 预算的损失统计与块级 scale 搜索。默认完全加权
（`_V_ATTENTION_IMPORTANCE_SHRINK = 1.0`）；实测收缩到 0.5 反而
稀释收益（+0.0002 vs +0.0010）。

### 实测（amax6，GPT-2 12 层）

相对 v1.4：attn 0.3746→0.3756（+0.0010），其余 7 项不变；
校准 +约 0.4s/12 层。

## v1.4 — 置换基扩展（weight-only / activation-only 排序候选）

### 大视角诊断

真实 GPT-2 数据上对 9 种排列基做候选度量对比，发现 `max(log range)`
组合排序并非最优：Linear 的 fc/proj 用 weight 侧排序（w_amax/w_rms）
代理误差低 5~9%，o 层甚至 identity 优于组合排序；Attention 的 K 侧
单边排序优于组合排序。块的 scale 搜索已饱和，但"哪些通道进同一个
64 块"仍有结构空间。

### 改动

`_PERMUTATION_BASES`：在选定的平滑 d 下，把 w_amax / x_amax / w_rms /
x_rms（Linear）与 q_amax / k_amax（Attention，逐 head）加入置换候选，
用与平滑候选相同的安全性门槛（均值 +2%、最坏 +0.5%）择优。

### 实测（amax6，GPT-2 12 层）

相对 v1.3：q +0.0009、v +0.0007、o +0.0002、k +0.0001，
fc/proj/attn 不变；校准 +约 140ms/层。完整贪心交换搜索未实施：
实测天花板（最终得分口径）只有 ~0.001，投入产出比不足。

## v1.3 — 动态激活的满 Gram 二次型加权（commit 4ee95c7）

### 大视角诊断

穷举全部 255 个 E6M2 码对比当前 scale 搜索（真实 GPT-2 数据）：
`exh/cur ≈ 1.000`——块级 scale 搜索已饱和，lv2/lv3/mantissa 已是精确解，
块内量化没有剩余空间。剩余误差全部来自结构层面：块成员（64 通道分组）、
排列、变换（平滑/居中）、跨算子耦合（重要性）。

### 改动

把权重侧已验证的满协方差二次型推广到**动态激活**：激活误差用
`W^T W` 的块内 4×4 Gram 加权（W 静态、Gram 稳定，不重蹈 Q/K 用少量
token 估计协方差的过拟合覆辙）。状态只存每 4 组 4×4 矩阵
（≈4×in_features 元素，单张量节点）；宽度上限 1024（元素数 ≤4096），
宽层（如 FFN 下投影 3072）自动回退对角重要性。

### 实测（amax6，GPT-2 12 层）

相对 v1.2：o 0.5021→0.5044（+0.0023）、fc +0.0007、q/k/v +0.0003~0.0005、
proj/Attention 不变。回归与 e2e 通过。

## v1.2 — 权重量化的满协方差二次型加权（commit e11e3c8）

### 数学动机

Linear 输出误差的精确目标是 `Δx^T G Δx`（G = W^T W 满 Gram），此前
的逐通道重要性只用了对角 `diag(G)`。真实激活相邻通道相关性 0.3~0.7，
且同一 4 组元素共享 scale/lv2/lv3，量化误差强相关，对角近似会系统性
选错 lv2/lv3。本轮在**权重量化**（离线、100% refine）中改用标定激活的
满块内协方差做二次型误差：

```
loss(块) = Σ_组 Δ^T G_组 Δ,   G_组 = E[x x^T] 的 4×4 块内子矩阵
```

协方差在标定时按最终 SmoothQuant/置换变换对齐，且**不进入任何动态
状态**，规避状态节点数限制（4096）风险；动态路径仍用对角重要性。

### 实测（真实 GPT-2，12 层，seq 128，权威 NVFP4 编码；得分越高越好）

| 编码 | Linear q | k | v | o | fc | proj | Attention |
| --- | --- | --- | --- | --- | --- | --- | --- |
| amax6（官方） | 0.4404→0.5900 | 0.3153→0.5746 | 0.5224→0.5893 | 0.2903→0.5021 | 0.2906→0.4646 | 0.0344→0.4009 | 0.2375→0.3746 |
| amax4（4/6） | +54.4% | +163.4% | +25.3% | +162.8% | +151.4% | +225.6% | +73.9% |
| pow2 | +30.2% | +78.5% | +18.8% | +112.2% | +77.9% | +165.2% | +78.9% |

相对 v1.1：o 0.4901→0.5021（+0.012）、proj 0.3922→0.4009（+0.009）、
fc +0.004、q/k/v +0.001~0.003；Attention 不变（权重与注意力无关）。
运行时只增加离线校准（约 +15%），动态量化成本不变。

### 同思路在 Q/K 上的负结果（记录，勿重复）

把同样的满协方差二次型推广到动态 Q/K 量化（Q 用 K 协方差、K 用 Q
协方差，4×4 组矩阵存状态，`_ATTN_QUADRATIC`）：attn 得分从 0.3746
崩到 0.2965。向对角收缩（λ=0.5/0.8）也救不回来（0.3066/0.3025）。
原因：Q/K 的 4×4 协方差仅由约 128 个标定 token 估计，方差大，求解器
会利用不稳定的组内相关"对消"误差，而 softmax 的指数敏感性放大过拟合。
结论：二次型只适用于离线权重（协方差估计稳、100% refine），动态
Q/K 保持对角重要性。相关代码已在清理时删除，完整实现保留在
git 历史（commit 86558f4 之前）。

## v1.1 — 数据驱动 refine 预算 + scale 边缘扩展（commit 待补）

### 数学动机

HiF4 每个 64 块在给定 E6M2 base scale 后，lv2/lv3/mantissa 的精确解
已经最优，块级剩余自由度只有 base scale。因此两个改进点：

1. **数据驱动 refine 预算**：全局手调比例是对所有层一视同仁，但各层
   块损失分布不同。现在标定时计算每层各块的加权标准损失，动态预算取
   "能覆盖 99% 总损失的最小块比例"（`_DATA_DRIVEN_RATIO`）。预算从
   拍脑袋参数变成标定统计量，各层自动分配。
2. **scale 边缘扩展**：块误差对 E6M2 码近似单峰，若最优解落在固定
   窗口边缘，说明真优解可能在窗口外。对落在边缘的块继续向外搜索
   1~2 档（`_REFINE_EDGE_EXTENSION`），大部分块预算不变。

### 尝试但放弃的改动

- **校准期按组件选择 offset**（`_OFFSET_SELECTION`）：统计每个 offset
  的损失贡献并按层保留 top-4。实测无正收益（+4 伤 proj 类层，选择本身
  略降 attn），已删除，完整实现保留在 git 历史（commit 86558f4 之前）。
- 动态 offset 池加回 -2、mean 居中、评估 token 数翻倍：均无提升。

### 实测（真实 GPT-2，12 层，seq 128，权威 NVFP4 编码；得分越高越好）

| 编码 | Linear q | k | v | o | fc | proj | Attention |
| --- | --- | --- | --- | --- | --- | --- | --- |
| amax6（官方） | 0.4404→0.5886 | 0.3153→0.5719 | 0.5224→0.5876 | 0.2903→0.4901 | 0.2906→0.4607 | 0.0344→0.3922 | 0.2375→0.3746 |
| amax4（4/6） | +53.5% | +161.1% | +24.7% | +152.4% | +148.7% | +219.5% | +73.9% |
| pow2 | +29.7% | +77.3% | +18.7% | +97.5% | +76.6% | +158.4% | +78.9% |

相对 v1.0（combo）：Attention 0.3410→0.3746，Linear 全线 +0.004~0.010。

### 运行时预算

动态量化：Q/K/V 约 9~10 ms/样本，激活约 10 ms/样本（数据驱动比例
普遍高于 v1.0）；注意力标定约 137 ms/层。N=10 组 × 16 测试样本外推
约 250 s，仍在 5 分钟预算内；若测试集极大（64+ 样本/组），需考虑
调低 `_RATIO_CAPTURE_TARGET`。

## v1.0 — RMS 平滑 + 绝对误差细化排序（commit 0a6b415）

### 算法改动

1. **RMS 平滑候选**：Linear 权重与 Attention Q/K 的 SmoothQuant 通道缩放，
   在原有的 amax 基础上新增一档 RMS（二阶矩）候选，对离群点更稳健；
   权重平滑 alpha 扩为 (0.25, 0.50, 0.75)。
2. **绝对误差细化排序**：refine 预算按块的绝对（重要性加权）重建误差
   排序选择，而非归一化误差——固定预算下贪心最大化输出 MSE 的降低。
3. **K 居中分位数变体**：新增 25/75 分位中点居中（softmax 精确不变性），
   实测未入选，代码保留但默认关闭。

### 参数变化（相对旧版）

- `_ACTIVATION_REFINE_MAX_RATIO`: 0.30 → 0.50
- `_Q/_K/_V_REFINE_MAX_RATIO`: 0.25/0.35/0.30 → 0.40/0.50/0.40
- `_WEIGHT_SMOOTH_ALPHAS`: (0.25, 0.50) → (0.25, 0.50, 0.75)
- 新增 `_WEIGHT_SMOOTH_RMS`、`_QK_SMOOTH_RMS`、`_REFINE_RANK_BY_ABSOLUTE`

### 实测（真实 GPT-2，12 层，seq 128，权威 NVFP4 编码；得分越高越好）

| 编码 | Linear q | k | v | o | fc | proj | Attention |
| --- | --- | --- | --- | --- | --- | --- | --- |
| amax6（官方） | 0.4404→0.5823 | 0.3153→0.5663 | 0.5224→0.5801 | 0.2903→0.4805 | 0.2906→0.4500 | 0.0344→0.3846 | 0.2375→0.3410 |
| amax4（4/6） | +50.6% | +157.6% | +22.0% | +145.5% | +138.0% | +213.5% | +55.5% |
| pow2 | +27.0% | +73.2% | +16.1% | +92.1% | +70.4% | +153.5% | +53.1% |

amax6 行给出 absolute 得分（旧→新），amax4/pow2 行为相对提升百分比。
全部 7 类场景为正提升；`test_solution.py` 全部通过。

### 运行时预算

激活动态量化约 11.6 ms/样本，Q/K/V 各约 6~7 ms/样本，权重校准
140~555 ms/层。按几十组 × 几十个测试样本外推，5 分钟预算余量充足。
